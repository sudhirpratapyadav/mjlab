# Task sequence definitions for continual learning experiments
#
# Each sequence is a list of task names defined in tasks.yaml.

sequences:
  sequence1: [LiftCube, OpenDoor, OpenDrawer, PushButton]
  sequence2: [OpenDoor, OpenDrawer, LiftCube, PushButton]
  sequence3: [LiftCube, PushButton]
  sequence4: [OpenDoor, OpenDrawer]
